kind: UDSBundle
metadata:
  name: helm-charts
  description: testing a bundle with Helm overrides
  version: 0.0.1

packages:
  - name: helm-charts
    path: "./zarf-package-helm-charts-amd64-0.0.1.tar.zst"
    ref: "0.0.1"

    overrides:
      demo-helm-charts:
        podinfo-local:
          # values:
          #   - path: "podAnnotations"
          #     value:
          #       bler: baz
          #       taco: bell
          variables:
            - path: "podAnnotations"
              name: POD_ANNOTATIONS
              default:
                hello: world
                doug: was-here
